global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: arkena-core-api
    metrics_path: /metrics
    scheme: http
    authorization:
      type: Bearer
      credentials_file: /run/secrets/metrics_bearer_token
    static_configs:
      - targets:
          - api:3000
